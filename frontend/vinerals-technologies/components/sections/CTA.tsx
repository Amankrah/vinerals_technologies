// This component is now empty because the CTA is integrated into the Footer component
// Pages that use <CTA /> will not show anything, as the Footer already includes the CTA section
const CTA = () => {
  return null;
};

export default CTA;
